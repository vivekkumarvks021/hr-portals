{
  "name": "hr-portal",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Configure later\"",
    "dev:shell": "npm run dev --workspace=shell",
    "dev:employee": "npm run dev --workspace=employee",
    "dev:leave": "npm run dev --workspace=leave",
    "dev:payroll": "npm run dev --workspace=payroll"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "typescript": "^5.9.2",
    "vite": "^7.3.6"
  }
}
